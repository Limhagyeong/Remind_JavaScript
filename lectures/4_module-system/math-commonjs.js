// exports.perfectScore = 100;

// exports.sum = (a, b) => {
//   return a + b;
// };

// exports.avg = (a, b) => {
//   return (a + b) / 2;
// };

// exports.subtract = (a, b) => {
//   return a - b;
// };

const perfectScore = 100;

const sum = (a, b) => {
  return a + b;
};

const avg = (a, b) => {
  return (a + b) / 2;
};

const subtract = (a, b) => {
  return a - b;
};

module.exports = {
  perfectScore,
  sum,
  avg,
  subtract,
};
